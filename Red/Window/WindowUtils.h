#pragma once

#include "../Core/Platform.h"

#if OS_WINDOWS
	#include "Windows/WindowsWindowUtils.h"
#endif

// WindowDescription is a typedef to a platform specific implementation.
// WindowState is a typedef to a platform specific implementation.